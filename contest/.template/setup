#!/usr/bin/env bash

# Flatten the Makefile: splice -include'd hook files (config.mk, local.mk)
# from layered templates into a single self-contained Makefile.
sed -i 's|^-include \(.*\)|cat \1 2>/dev/null|e' Makefile
rm -f config.mk local.mk

bear -- make all
